# A web application for Online Auction

## How to use

First, we need to clone the project into your computer with the following command

```
git clone https://github.com/todanh2706/WebDev_Project.git
```

After that, we need to install all required packages which are defined in `package.json`

```
cd frontend; npm install; cd ..; cd backend; npm install
```
